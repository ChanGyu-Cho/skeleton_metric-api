# Requirements for skeleton_metric-api service
# Use unpinned package names by default to reduce version conflict risk.
# Pin a specific version only when absolutely necessary.

# CRITICAL: PyTorch should be installed FIRST via pip install command in Dockerfile
# with explicit CUDA version (--index-url https://download.pytorch.org/whl/cu121)
# to ensure compatibility with OpenPose CUDA 12.x runtime.
# DO NOT list torch/torchvision here - they are installed separately in Dockerfile.

fastapi
uvicorn
numpy
pandas
pyyaml
# Use headless OpenCV in container environments. For local GUI development you may
# prefer `opencv-python` instead of `opencv-python-headless`.
opencv-python-headless

boto3

# for stgcn-api request
requests

# YOLO for person detection and cropping (matches local preprocessing)
# ultralytics will use the pre-installed PyTorch from Dockerfile
ultralytics
scikit-learn  # required for clustering in YOLO crop logic